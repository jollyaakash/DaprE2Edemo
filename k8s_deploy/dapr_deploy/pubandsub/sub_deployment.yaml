apiVersion: apps/v1
kind: Deployment
metadata:
  name: dapr-subscriber
  labels:
    app: subscriber
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subscriber
  template:
    metadata:
      labels:
        app: subscriber
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "orderprocessing"
        dapr.io/app-port: "6001"
        dapr.io/app-protocol: "http"
        dapr.io/log-level: "debug"
    spec:
      containers:
      - name: subscriber
        image: dmqttdemo.azurecr.io/dapr/pubsubexample_sub:latest
        volumeMounts:
          - name: iotedge-spiffe-agent-socket
            mountPath: /run/iotedge/sockets/
      initContainers:
      - name: set-ownership
        image: busybox:latest
        command: ["sh", "-c", "chmod 666 /run/iotedge/sockets/workloadapi.sock"]
        volumeMounts:
          - name: iotedge-spiffe-agent-socket
            mountPath: /run/iotedge/sockets/
      volumes:
      - name: iotedge-spiffe-agent-socket
        hostPath:
          path: /run/iotedge/sockets/
          type: DirectoryOrCreate
